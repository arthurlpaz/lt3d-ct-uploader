# Copie para .env e preencha. O docker compose lê o .env automaticamente.
UPLOAD_USER=protesia
UPLOAD_PASS=troque-esta-senha

# Pasta raw_data no host (opcional). Padrão: ./raw_data
# RAW_DATA_HOST=/data/protesia/raw_data
